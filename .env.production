# Production Environment Configuration
# Set these as environment variables in your deployment platform (Vercel, Railway, etc.)

# Database - PostgreSQL connection string from your production database
DATABASE_URL="postgresql://user:password@host:5432/mindmap_prod"

# NextAuth Configuration - MUST match your production domain
NEXTAUTH_URL="https://yourdomain.com"
NEXTAUTH_SECRET="REPLACE_WITH_SECURE_SECRET_MIN_32_CHARS"

# Encryption for LLM API keys - MUST be secure and kept secret
ENCRYPTION_KEY="REPLACE_WITH_SECURE_KEY_MIN_32_CHARS"

# Redis (recommended for production)
REDIS_URL="redis://user:password@host:6379"

# Web search providers (optional)
# TAVILY_API_KEY="your-tavily-api-key"
# SERPAPI_API_KEY="your-serpapi-api-key"
# BING_SEARCH_API_KEY="your-bing-api-key"

# App Configuration
NEXT_PUBLIC_APP_NAME="MindMap"

# CORS Configuration (optional)
# ALLOWED_ORIGINS="https://yourdomain.com,https://www.yourdomain.com"

# Security Headers (recommended)
# These are typically configured in your deployment platform
